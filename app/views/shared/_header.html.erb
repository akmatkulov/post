<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand fs-5 fw-bold" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Posts", articles_path, class: "nav-link active fs-5 fw-bold" %>
        </li>
        <li class="nav-item">
          <%= link_to "New Article", new_article_path, class: "nav-link active fs-5 fw-bold" %>
        </li>
      </ul>

      <div class="btn-group navbar-nav ms-auto mb-2 mb-lg-0" role="group" aria-label="Basic outlined example">
        <% if user_signed_in? %>
          <%= link_to "Sign out", destroy_user_session_path,  data: { "turbo-method": :delete }, class: "btn btn-outline-primary", type: "button" %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "btn btn-outline-primary", type: "button" %>
          <%= link_to "Sing up", new_user_registration_path, class: "btn btn-outline-primary", type: "button" %>
        <% end %>
      </div>

    </div>
  </div>
</nav>
